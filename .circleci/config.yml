---
version: 2.1

orbs:
  orb: suzuki-shunsuke/orb@0.1.2
  # aqua-installer: suzuki-shunsuke/aqua-installer@0.1.0

workflows:
  build:
    jobs:
      - orb/pack:
          name: pack
          filters:
            tags:
              only: /.*/
      - orb/test:
          name: test
          context: suzuki-shunsuke
          workflows: ls tests/*
          requires:
            - pack
          filters:
            tags:
              only: /.*/

      - publish-dev:
          context: suzuki-shunsuke
          requires:
            - pack
            - test

      - publish:
          context: suzuki-shunsuke
          requires:
            - pack
            - test
          filters:
            tags:
              only: /.*/
            branches:
              ignore: /.*/
executors:
  circleci:
    docker:
      - image: circleci/circleci-cli:0.1.15973-alpine
jobs:
  publish-dev:
    executor: circleci
    steps:
      - orb/publish-dev:
          orb_path: suzuki-shunsuke/aqua
  publish:
    executor: circleci
    steps:
      - orb/publish:
          orb_path: suzuki-shunsuke/aqua
